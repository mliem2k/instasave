org.gradle.caching = true
org.gradle.parallel = true
android.useAndroidX = true
kotlin.code.style = official
version = 0.2.11

# Provide your own GitHub Packages credentials locally (do NOT commit real values):
# gpr.user=<your-github-username>
# gpr.key=<a classic PAT with read:packages scope>
# Alternatively, export GITHUB_ACTOR / GITHUB_TOKEN in your shell before building.
