.gradle/
build/
out/
*.iml
.idea/
.DS_Store
local.properties
*.apk
*.keystore
*.jks
gradle.properties.local

# Design and planning scratch. Never committed, in any repo.
docs/superpowers/
.superpowers/

# Anchor verification working directory: a decompiled Instagram APK is large and
# is not redistributable.
tools/.work/

# Python bytecode cache from tools/
__pycache__/
*.pyc

# Generated by the generatePatchesList task. Regenerate with:
#   ./gradlew :patches:generatePatchesList
# Not committed: nothing in this repo updates it automatically, so a committed copy would
# silently go stale and misreport what the bundle offers.
patches-list.json
patches-bundle.json

# Release staging. Holds a patched Instagram binary and a built bundle;
# neither belongs in git.
release/
